#include <iostream>

using namespace std;

int main() {
    int H, W, M;
    cin >> H >> W >> M;

    for (int i = 0; i < M; i++) {
        int X, Y;
        cin >> X >> Y;

    }

    // 行、列ごとに限界の場所（最初は端っこ）を持っておく 10^5
    // 到達できない条件はLルートx2が両方閉ざされること
}